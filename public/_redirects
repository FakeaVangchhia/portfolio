# Netlify / Cloudflare Pages SPA fallback.
#
# React Router owns /resume and /neural_visual. Without this, loading either URL
# directly (or refreshing on it) asks the host for a file that does not exist
# and returns 404 — the routes only work when reached by in-app navigation.
# Real files in dist/ are served first; everything else falls through to the app.
/*    /index.html   200
